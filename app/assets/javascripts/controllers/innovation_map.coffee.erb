'use strict'

Application.Controllers.controller "InnovationMapController", ["$scope", "$rootScope", "$state", "$window", "_t", "CSRF", "Session"
, ($scope, $rootScope, $state, $window, _t, CSRF, Session) ->

  ### PUBLIC SCOPE ###

  ### PRIVATE SCOPE ###

  ##
  # Kind of constructor: these actions will be realized first when the controller is loaded
  ##
  initialize = ->

  ## !!! MUST BE CALLED AT THE END of the controller
  initialize()

]
